# Zeige verfügbare Zimmer
available_rooms = get_available_rooms_by_hotel(selected_hotel.hotel_id, today)
print("\nAvailable rooms today:")
if available_rooms:
    for room in available_rooms:
        print(f" - Room {room.number}, CHF {room.price_per_night:.2f} per night")
else:
    print(" No available rooms today.")

# Zeige nächstes freies Datum
next_date = get_next_available_date_for_hotel(selected_hotel.hotel_id)
if next_date:
    print(f"Next available date for any room: {next_date}")
else:
    print("No upcoming availability found.")

#Immer importieren
import sys
import os
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
from business_logic.hotel_manager import find_all_hotel_details, remove_hotel